angular.module('wsss.config', [

])

# service
# ------------------------------------------------------------
.factory('ConfigService', [
  '$http'
  '$log'
  ($http, $log)->

    getData = (url) ->

    {
      getData
    }
])